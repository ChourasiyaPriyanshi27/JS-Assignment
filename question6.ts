let string: string = "leena";
let s: string = string.charAt(0).toUpperCase() + string.slice(1); // Convert string 1st letter into Uppercase
console.log(s);
export default { string, s };
// Output : Leena
